pipeline {
  agent none
  stages {
    stage('Build AppJar') {
      agent {
        docker { image 'maven:3.8.5-openjdk-17' }
      }
      steps {
        sh 'mvn --version'
        sh 'java --version'
        sh './mvnw package'
      }
    }
  }
}
